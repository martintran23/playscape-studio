# Copy to `.env` and add your public token from https://account.mapbox.com/
VITE_MAPBOX_TOKEN=
VITE_MAPBOX_ACCESS_TOKEN=

# Optional: show a 1m red cube in the scene to verify scale (main vs focus)
# VITE_DEBUG_SCALE_CUBE=1
